# data & training artifacts (rebuilt by scripts/, weights live on HF Hub / Colab)
# anchored to repo root so turnwave/data/ (source code) is not ignored
/data/
/checkpoints/
/runs/

# secrets
.env

# python
__pycache__/
*.pyc
.venv/
.pytest_cache/
*.egg-info/
dist/

# downloaded training artifacts
*.zip

# generated model card (rebuilt by scripts/publish_hf.py)
docs/model_card.md
